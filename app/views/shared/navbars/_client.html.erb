<nav class="navbar navbar-expand-lg bg-white py-3 navbar-soft">
  <div class="container">

    <%= render "shared/logo" %>

    <div class="ms-auto d-flex align-items-center">

      <div class="dropdown">
        <%= link_to "Home", root_path, class: "btn btn-soft btn-pill" %>

        <button class="btn-primary btn-pill"
                type="button"
                data-bs-toggle="dropdown"
                aria-expanded="false">

          <i class="bi bi-people"></i>
        </button>

        <ul class="dropdown-menu dropdown-menu-end">

          <li class="px-3 py-2 d-flex align-items-center gap-2">
            <div class="avatar-btn" style="width:32px; height:32px; font-size:0.8rem;">
              <%= current_user.email.first.upcase %>
            </div>
            <div>
              <div class="fw-semibold"><%= current_user.email %></div>
              <small class="text-muted">Cliente</small>
            </div>
          </li>

          <li><hr class="dropdown-divider"></li>

          <li>
            <%= link_to "Ver detalhes", clients_profile_path, class: "dropdown-item" %>
          </li>

          <li>
            <%= link_to "Ver planos", "#", class: "dropdown-item" %>
          </li>

          <li><hr class="dropdown-divider"></li>

          <li>
            <%= button_to "Sair",
              destroy_user_session_path,
              method: :delete,
              class: "dropdown-item text-danger" %>
          </li>
        </ul>
      </div>
    </div>
  </div>
</nav>