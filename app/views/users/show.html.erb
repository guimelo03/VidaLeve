<div class="container-lg py-5">

  <div class="mb-4">
    <%= link_to clients_dashboard_path, class: "btn btn-soft btn-pill" do %>
      <i class="bi bi-arrow-left"></i>
      Voltar
    <% end %>

    <%= link_to edit_user_profile_path,
      class: "btn btn-primary btn-pill" do %>
      <i class="bi bi-pencil"></i>
      Editar
    <% end %>
  </div>

  <div class="row">
    <div class="col-lg-8">

      <div class="row">
        <div class="col-md-6 mb-4">
          <%= render "users/profile_section", title: "Dados pessoais" do %>

            <%= render "users/profile_field", label: "Telefone", value: format_phone(@user.phone) %>
            <%= render "users/profile_field", label: "Idade", value: @user.age %>
            <%= render "users/profile_field", label: "Sexo", value: @user.sex&.humanize %>
            <%= render "users/profile_field", label: "Cidade", value: "#{@user.city} - #{@user.state}" %>
            <%= render "users/profile_field", label: "Profissão", value: @user.profession %>
            <%= render "users/profile_field", label: "Rotina", value: @user.work_routine %>

          <% end %>
        </div>

        <div class="col-md-6 mb-4">
          <%= render "users/profile_section", title: "Informações fisiológicas" do %>

            <%= render "users/profile_field", label: "Peso", value: "#{@user.current_weight} kg" %>
            <%= render "users/profile_field", label: "Altura", value: "#{@user.height} m" %>
            <%= render "users/profile_field", label: "Circunferência", value: "#{@user.abdominal_circumference} cm" %>

            <%= render "users/profile_field",
              label: "Mediu com fita",
              value: @user.measured_with_tape ? "Sim" : "Não" %>

            <% if @user.measurement_notes.present? %>
              <%= render "users/profile_field",
                label: "Observação",
                value: @user.measurement_notes %>
            <% end %>

            <%= render "users/profile_field",
              label: "Objetivo",
              value: @user.main_goal&.humanize %>

          <% end %>
        </div>

        <div class="col-12">
          <%= render "users/profile_section", title: "O que mais incomoda" do %>
            <p><%= @user.main_discomfort.presence || "-" %></p>
          <% end %>
        </div>

      </div>
    </div>

    <div class="col-lg-4 d-flex justify-content-end">
      <div class="profile-sidebar me-4">

        <div class="avatar-large mb-3">
          <i class="bi bi-person"></i>
        </div>

        <h4 class="fw-semibold mb-1">
          <%= @user.full_name.presence || @user.email %>
        </h4>

        <p class="text-muted small">
          <%= @user.email %>
        </p>

        <% unless @user.has_complete_info? %>
          <div class="mt-2 mb-2">
            <%= link_to "Completar cadastro", onboarding_path, class: "btn btn-primary btn-sm" %>
          </div>
        <% end %>

        <span class="profile-status <%= @user.has_complete_info? ? 'complete' : 'incomplete' %>">
          <%= @user.has_complete_info? ? "Cadastro completo" : "Cadastro incompleto" %>
        </span>

      </div>
    </div>
  </div>

</div>